{
  "version": 2,
  "buildCommand": "cd frontend && npm install && npm run build && cd .. && rm -rf public && cp -r frontend/dist public",
  "outputDirectory": "public",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
